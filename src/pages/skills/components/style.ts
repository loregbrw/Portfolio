import styled from "styled-components";

export const StyledSkill = styled.div`
    width: 100%;

    display: flex;
    flex-direction: column;

    gap: 3px;
`

export const StyledS = styled.span`
    text-align: justify;

    display: flex;
    flex-wrap: wrap;
`

export const StyledName = styled.span`
    font-weight: 400;
    font-weight: 800;

    margin-right: 10px;
`